@using OrdersADS.Models
@model OrdersADS.Models.Status

@{
    ViewBag.Title = "Информация";
}

<h2>О статусе</h2>

<div>
    <h4>Статус</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.StatusName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.StatusName)
        </dd>

    </dl>
</div><div class="display-label"><b>Заявки</b></div>
<ul>
    @foreach (Request o in Model.Requests)
    {
        <li>@o.Name</li>
    }
</ul>
<p>
    @Html.ActionLink("Изменить", "Edit", new { id = Model.Id }, new {@class = "btn btn-primary") |
    @Html.ActionLink("Обратно", "Index", null, new { @class = "btn btn-default"})
</p>
